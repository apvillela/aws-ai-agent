{
  "name": "sentinel-ingest",
  "version": "1.0.0",
  "description": "Sentinel Ingest Lambda — validates and queues lead payloads",
  "main": "handler.js",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.738.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.13.5",
    "@types/uuid": "^10.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
